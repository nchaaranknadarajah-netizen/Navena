# Navena

This repository contains the Navena single-page web app (HTML/CSS/JS).

The initial commit provides the app shell and UI. A feature branch `fix/safe-render-supabase` will contain safety and Supabase improvements (safe DOM rendering, upload error handling, accessibility fixes, and other polish).